import { Text, type TextProps } from "@chakra-ui/react";

export default function PageSubtitle(props: TextProps) {
  return <Text {...props} fontSize="0.93rem" fontWeight="500" color="gray.500" />;
}
